                <div class="flex-shrink-0">
                    <div class="flex items-center">
                        <a href="/" class=" flex items-center">
                            <% if (typeof isLandingPage !== 'undefined' && isLandingPage === true) { %>
                                <!-- Show logo image on landing page -->
                                <img src="/placeholder/J.png" alt="JCCbuzz Logo">
                            <% } else { %>
                                <!-- Show text on other pages -->
                                <span class="font-bold text-2xl text-gray-900 dark:text-white transition-colors duration-200">JCCbuzz</span>
                            <% } %>
                        </a>
                    </div>
                </div> 